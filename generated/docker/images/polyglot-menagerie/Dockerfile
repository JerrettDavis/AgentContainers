# AUTO-GENERATED — do not edit by hand.
# Curated publish target: polyglot-menagerie
# Generated by AgentContainers.Generator v0.1.0

ARG BASE_IMAGE=agentcontainers/ac-fullstack-polyglot:latest
FROM ${BASE_IMAGE}

USER root

# Install Claude Code
RUN npm install -g @anthropic-ai/claude-code
# Verify Claude installation
RUN claude --version

# Install OpenAI Codex CLI
RUN npm install -g @openai/codex
# Verify Codex installation
RUN codex --version

# Install GitHub Copilot CLI
RUN npm install -g @githubnext/github-copilot-cli
# Patch vulnerable semver dependency
RUN npm install --omit=dev --prefix /usr/local/lib/node_modules/@githubnext/github-copilot-cli/node_modules/simple-update-notifier semver@7.7.2
# Verify Copilot CLI installation
RUN github-copilot-cli --version || true

# Install OpenClaw
RUN npm install -g openclaw
# Patch vulnerable axios dependency
RUN npm install --omit=dev --prefix /usr/local/lib/node_modules/openclaw/dist/extensions/feishu axios@1.15.0
# Patch nested feishu axios dependency
RUN npm install --omit=dev --prefix /usr/local/lib/node_modules/openclaw/dist/extensions/feishu/node_modules/@larksuiteoapi/node-sdk axios@1.15.0
# Patch vulnerable tar dependency
RUN npm install --omit=dev --prefix /usr/local/lib/node_modules/openclaw/dist/extensions/discord tar@7.5.11
# Patch nested discord tar dependency
RUN npm install --omit=dev --prefix /usr/local/lib/node_modules/openclaw/dist/extensions/discord/node_modules/@discordjs/node-pre-gyp tar@7.5.11
# Verify OpenClaw installation
RUN openclaw --version || true

# Install system-level dev/debug/build/productivity tools
RUN apt-get update && apt-get install -y --no-install-recommends \
  shellcheck \
  yamllint \
  clang-format \
  valgrind \
  strace \
  gdb \
  cmake \
  make \
  build-essential \
  lldb \
  lsof \
  net-tools \
  dnsutils \
  iproute2 \
  tcpdump \
  httpie \
  silversearcher-ag \
  bat \
  fzf \
  tmux \
  entr \
  watchman \
  socat \
  file \
  patchelf \
&& rm -rf /var/lib/apt/lists/*
# Install JS/TS dev tools globally
RUN npm install -g prettier eslint typescript ts-node @biomejs/biome turbo
# Install Python dev/lint tools
RUN python3 -m pip install --break-system-packages --no-cache-dir ruff mypy black isort bandit pre-commit cookiecutter

# Environment defaults
ARG CLAUDE_CONFIG_DIR=/home/dev/.config/claude
ENV CLAUDE_CONFIG_DIR=${CLAUDE_CONFIG_DIR}
ARG CLAUDE_WORKSPACE=/workspace
ENV CLAUDE_WORKSPACE=${CLAUDE_WORKSPACE}
ARG CODEX_HOME=/home/dev/.config/codex
ENV CODEX_HOME=${CODEX_HOME}
ARG CODEX_WORKSPACE=/workspace
ENV CODEX_WORKSPACE=${CODEX_WORKSPACE}
ARG COPILOT_CONFIG_DIR=/home/dev/.config/github-copilot
ENV COPILOT_CONFIG_DIR=${COPILOT_CONFIG_DIR}
ARG COPILOT_WORKSPACE=/workspace
ENV COPILOT_WORKSPACE=${COPILOT_WORKSPACE}
ARG OPENCLAW_PORT=3000
ENV OPENCLAW_PORT=${OPENCLAW_PORT}
ARG OPENCLAW_CONFIG_DIR=/home/dev/.config/openclaw
ENV OPENCLAW_CONFIG_DIR=${OPENCLAW_CONFIG_DIR}
ARG DEVTOOLS_ENABLED=1
ENV DEVTOOLS_ENABLED=${DEVTOOLS_ENABLED}

USER dev

HEALTHCHECK --interval=30s --timeout=10s --retries=3 \
  CMD ["CMD-SHELL", "claude --version || exit 1"]

# OCI Image Labels
ARG BUILD_DATE=unknown
ARG VCS_REF=unknown
ARG IMAGE_VERSION=0.1.0

LABEL org.opencontainers.image.title="Polyglot agent menagerie"
LABEL org.opencontainers.image.description="Full-stack image with multiple agent CLIs and developer tools, published through stack-first and bundle-first aliases"
LABEL org.opencontainers.image.source="https://github.com/agentcontainers/AgentContainers"
LABEL org.opencontainers.image.vendor="AgentContainers"
LABEL org.opencontainers.image.licenses="MIT"
LABEL org.opencontainers.image.version="${IMAGE_VERSION}"
LABEL org.opencontainers.image.created="${BUILD_DATE}"
LABEL org.opencontainers.image.revision="${VCS_REF}"
LABEL dev.agentcontainers.image-type="tag-policy"
LABEL dev.agentcontainers.runtime="fullstack-polyglot"
LABEL dev.agentcontainers.agents="claude,codex,copilot,openclaw"
LABEL dev.agentcontainers.tool-packs="devtools"
LABEL dev.agentcontainers.release-version="0.1.0"
LABEL dev.agentcontainers.runtime-display-name="Full-Stack Polyglot"
