# AUTO-GENERATED — do not edit by hand.
# Tool pack overlay: devtools on fullstack-polyglot
# Generated by AgentContainers.Generator v0.1.0

ARG BASE_IMAGE=agentcontainers/fullstack-polyglot:latest
FROM ${BASE_IMAGE}

USER root

# Install Developer Power Tools
# Install system-level dev/debug/build/productivity tools
RUN apt-get update && apt-get install -y --no-install-recommends \
  shellcheck \
  yamllint \
  clang-format \
  valgrind \
  strace \
  ltrace \
  gdb \
  cmake \
  make \
  build-essential \
  lldb \
  lsof \
  net-tools \
  dnsutils \
  iproute2 \
  tcpdump \
  httpie \
  silversearcher-ag \
  bat \
  fzf \
  tmux \
  entr \
  watchman \
  socat \
  file \
  patchelf \
&& rm -rf /var/lib/apt/lists/*
# Install JS/TS dev tools globally
RUN npm install -g prettier eslint typescript ts-node @biomejs/biome turbo
# Install Python dev/lint tools
RUN python3 -m pip install --break-system-packages --no-cache-dir ruff mypy black isort bandit pre-commit cookiecutter

# Environment defaults
ARG DEVTOOLS_ENABLED=1
ENV DEVTOOLS_ENABLED=${DEVTOOLS_ENABLED}

USER dev

# OCI Image Labels
ARG BUILD_DATE=unknown
ARG VCS_REF=unknown
ARG IMAGE_VERSION=v1

LABEL org.opencontainers.image.title="Developer Power Tools on Full-Stack Polyglot"
LABEL org.opencontainers.image.description="Batteries-included developer tooling pack — linters, formatters, debug utilities, build helpers, and productivity CLIs"
LABEL org.opencontainers.image.source="https://github.com/agentcontainers/AgentContainers"
LABEL org.opencontainers.image.vendor="AgentContainers"
LABEL org.opencontainers.image.licenses="MIT"
LABEL org.opencontainers.image.version="${IMAGE_VERSION}"
LABEL org.opencontainers.image.created="${BUILD_DATE}"
LABEL org.opencontainers.image.revision="${VCS_REF}"
LABEL dev.agentcontainers.image-type="tool-pack"
LABEL dev.agentcontainers.tool-pack="devtools"
LABEL dev.agentcontainers.base="fullstack-polyglot"
